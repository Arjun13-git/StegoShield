# Base URL of the StegoShield FastAPI backend, as seen from the BROWSER.
# NEXT_PUBLIC_* values are embedded in the client bundle: never put secrets here.
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
